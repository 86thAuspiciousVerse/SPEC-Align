# Spec Align 个人版实施与验收计划

状态：v0.3 本地实现与验收完成；新 Codex CLI 模型送达已实测，当前桌面 PostToolUse 仍未观察到触发。日期：2026-09-14。依据：[当前设计](SPEC-ALIGN-CURRENT.md)。

## 交付目标与边界

交付一个可安装的本地工具，支持显式 Markdown 条目和依赖、当前依据与历史复核、变化影响和精简上下文、Codex 项目接入、MCP 调用、备份恢复及提交检查。纯代码运行，不推断遗漏的语义依赖，不分析对话。用户无需记住管理流程，由随包 skill 指导 agent，宿主 hook 提供检查触发机会。

代码功能完成、自动测试通过、宿主模型真正收到提醒是三个独立状态。不能用模拟 hook 验证替代 Codex 实测。若独立 CLI 认证仍不可用，完成所有本地工作，明确记录这一项发布限制；不长时间追查外部认证。

本轮不做：图形编辑器、Web 服务、跨仓库同步、多模型语义审查、完整宿主适配生态。图查看以 JSON 与 Mermaid 导出提供。不要求每条决定都向用户审批；采纳命令记录显式操作者与理由，权限由用户和 agent 工作流负责。

## 实现顺序与验收

| 阶段 | 工作 | 验收条件 | 状态 |
| --- | --- | --- | --- |
| M1 稳定核心 | 独立协议/配置；内容 hash 解析缓存；稳定问题 ID/生命周期；快照复核；备份恢复 | 旧示例兼容；无变化不重解析；变更/删除/解析失败/并发场景可验证；问题可复发并保留历史；备份恢复保留审查 | 完成 |
| M2 决策与审计 | proposed/active/retired；显式 supersedes/challenges；采纳/退役命令；审计查询 | 提案不自动覆盖现行决定；采纳必须绑定快照与理由；替代可追溯；重复替代/关系错误可检测；旧文档无需批量重写 | 完成 |
| M3 Agent 查询 | impact、graph、按条目 context；check --agent；解释中的旧/新证据 | 只沿 depends_on 传播复核；上下文显示来源及未解决问题；截断明确；图边完整；命令退出码有合同 | 完成 |
| M4 Codex 接入 | 项目 hook/skill 安装卸载；诊断；可配置 Stop 把关；可选 Git pre-commit；事件适配独立 | 保留已有配置；幂等；不自动信任 hooks；无关项目静默；重复事件不刷屏；Stop 最多续一次；模拟回路通过；真实送达单列 | 本地通过；真实送达待实测 |
| M5 MCP 与分发 | 标准 MCP stdio server；复用核心查询和复核；wheel 包含 skill；完整 CLI | initialize/list/call 可用；stdout 只有协议消息；无效参数结构化报错；安装产物可在仓库外运行；查询不伪造复核 | 完成 |
| M6 综合交付 | 场景测试、独立审查、文档收口、真实项目自用样例、Git 提交 | 全套测试和可执行演示通过；独立审查问题处理完；当前文档不互相矛盾；验收报告列明已完成/外部未验证项；Git 干净 | 完成 |

## 关键设计决策

- Python + Markdown AST + YAML + SQLite，沿用现有原型；核心和 Codex/MCP/CLI 适配分离。
- Markdown 是当前声明源，SQLite 保留快照与审计。自动扫描不得刷新审查基线。备份是显式工具能力，不把账本当缓存。
- 内容 hash 扫描保证能发现同时间戳/同大小修改；缓存只跳过解析，不能仅用 mtime 推断未变化。解析失败保留最后成功快照，同时显式报告未知状态。
- 配置只声明受管目录、排除模式、提醒与门槛；路径限定项目内，不执行文档代码。
- 关系由作者声明；普通 references 不传播失效，depends_on 传播；supersedes 仅由已采纳条目生效，challenges 保留待处理意见，不由工具选择谁正确。
- 多个调用用 SQLite 写事务串行化；对审查快照做一致性校验。无法锁住外部编辑器的全部写入，检测到扫描期间变化时拒绝审查并要求重试。
- 阻塞策略区分结构错误与待复核警告。不能靠无限 Stop 续回合逼迫用户接受决定。
- 不把任何远程创建、推送或发布作为本计划的隐含动作。

## 工作与验收纪律

每阶段由主 agent 实施，必要测试通过后交给新上下文做只读独立审查；修复实际问题后提交 Git。探索可以并行，执行不委托。阶段中发现约束不成立时在本计划记录调整及证据，不静默换目标。

## 进展与证据

- 起点：原型 14 项测试，CLI walkthrough 可运行，skill 可被发现；真正 Codex hook 送达仍未验证。
- 2026-09-14：完成 M1–M5 代码及 M6 本地验收；v0.2 测试由 14 项扩展至 34 项，随后 v0.3 扩展至 43 项。核心与集成经过隔离上下文审查，发现问题已修复并复查。
- 仓库外 wheel 安装、真实 MCP stdio 握手、生成的 hook 命令执行、自用条目复核完成；这些不等于 Codex 模型送达。
- 接受替代后允许下游暂时出现无效依赖，由显式迁移收口；防止采纳者直接依赖被自己替代的目标。此边界已写入正式协议并测试。
- 交付与未验证边界详见 [ACCEPTANCE.md](ACCEPTANCE.md)。
- 2026-09-16：修复全局固定根目录 MCP 的隔离风险。服务支持无参数未绑定模式，启动时不扫描目录，所有项目调用显式提供绝对 `root` 并返回 `project_root`；CC Switch 中的全局条目已备份后改为 `serve` 并保持停用，项目级绑定模式仍兼容。

## 0.3：像素转换器实验反馈改进（2026-09-14）

范围经用户确认：紧凑输出 → 批量复核 → 范围查询与迁移预演。不做跨文件批量采纳、自然语言 lint、全局账本或代码测试关联。

| 顺序 | 实现 | 验收 | 状态 |
| --- | --- | --- | --- |
| 1 | MCP check/写回执默认摘要，full 按需，CLI兼容 | 真实 MCP 验证摘要无 items、full 有正文；44条目项目摘要不携正文 | 已实现，通过 |
| 2 | review_batch 逐项理由、统一事务与 batch 审计 | 无效目标整批拒绝；中途文件变化回滚全部审查及batch映射；成功逐项可追溯 | 已实现，通过 |
| 3 | scope/since_snapshot、多ID有限关系上下文、迁移清单 | 跨scope结构错误不隐藏；删除/范围移动可比较；候选循环标识；无文件修改 | 已实现，通过 |
| 4 | skill/协议/分发及独立审查 | 39测试、skill验证、仓库外wheel安装通过；新上下文独立审查无阻塞 | 完成 |

不使用本轮新工具为未读内容批量登记复核；像素实验现有最终快照及审查记录保持不变。用户已有实验文件与暂存删除不纳入本轮处理。

