# Spec Align 当前设计讨论记录

> 状态：当前设计基线（讨论记录，尚非冻结的实现规格）
> 更新日期：2026-09-14
> 来源：用户与 Agent 围绕个人开发工作流的本轮讨论。
> 阅读入口：后续设计与实现应首先阅读本文。
> 旧版 MVP 已移入 `archive/2026-09-14-previous-mvp/`，仅用于历史追溯，不作为当前实现任务清单。

## 实现进度补记（2026-09-14，v0.3）

当前已实现个人版 v0.3：文档协议、配置、解析缓存、依赖与生命周期检查、版本复核、审计、备份恢复、影响图与上下文、Codex 项目安装器、Git 暂存检查、标准 MCP stdio 服务、批量复核、范围/快照查询、迁移预演和 Windows 单文件分发脚本。

- 当前实现合同以 [docs/PROTOCOL.md](docs/PROTOCOL.md) 为准；使用见 [README.md](README.md)，实施与验收见 [IMPLEMENTATION-PLAN.md](IMPLEMENTATION-PLAN.md)、[ACCEPTANCE.md](ACCEPTANCE.md)。
- 下文保留原始讨论及当时的待定项，不再用其中的“尚未实现/待定”描述当前发布状态。协议、状态、存储、阈值和 API 已在正式协议中确定。
- Codex hook 的命令与 JSON 协议已测试；用户信任后，新 Codex CLI 的模型送达及复核清零后静默已实测通过。当前桌面会话的原生 MCP 已通过，但桌面 PostToolUse 尚未观察到触发；不能把 CLI 证据直接代替桌面验证。
- SQLite 同时存放索引和不可自动重建的复核账本，提供 backup/restore；不可当缓存清理。代码和 Markdown 使用本地 Git 管理，账本另行备份。
- 后续用户已授权只读研究 codegraph 并实施系统；下文最初“不看源码”的约束仅描述当时讨论阶段。codegraph 始终不属于本仓库的修改或提交范围。

## 1. 目标与使用场景

Spec Align 是个人使用的文档依赖与状态管理工具，主要服务于使用 coding agent 开发项目的过程。首先适配 Codex，架构保留其他 harness 的适配能力；不预设各宿主拥有相同的 hook 能力。

用户通常先表达模糊想法，由 agent 通过类似 grill-me 的追问澄清需求，然后开展调研、绘制架构与数据流、规划里程碑，并在各模块实施前派 subagent 探路。探路报告可能推翻早期技术假设，造成设计、任务和其他报告过时。

工具也必须支持宽松的玩具项目：用户可以让 agent 自主决定大部分工程事项，不必逐步审批或记住文档维护流程。流程记账由配套 skill 指导 agent 完成。

目标是持续回答：

- 当前以哪些结论为准？
- 这些结论依赖什么，为什么生效？
- 哪些旧结论已被替代？
- 本次变化还有哪些已声明的依赖没有完成复核？

允许探索推翻假设。历史报告可以保留当时的结论，当前执行入口必须区分现行依据与历史材料。

## 2. 已确认的产品边界

- runtime 为纯代码，不内置 LLM，不理解自然语言冲突，不生成语义建议。
- runtime 只管理已经声明的关系，不自动推断或补全真实依赖。
- 不读取或分析 agent 聊天输出来猜测工作意图。
- runtime 的输入来自文件状态、宿主提供的明确事件和 agent 显式工具调用。
- skill 使用正在开发项目的 agent 完成语义理解、依赖声明和复核；这不等于向 runtime 集成模型。
- 自动化覆盖扫描、结构校验、状态保存、影响计算、提醒与复核记录校验，不承诺自动完成语义对齐。
- 不查看或修改 codegraph 源码是本轮讨论与记录工作的边界；尚未据其源码决定技术实现。

## 3. 组成与职责

| 部分 | 职责 |
| --- | --- |
| 文档协议 | 定义重要条目的身份、显式关系、生命周期与版本复核语义 |
| 配套 skill | 指导 agent 编写条目、声明依赖、归并探路结论、在授权范围内决策和复核 |
| runtime 核心 | 解析、扫描、校验、构图、版本比较、持久化问题与复核记录、生成精简结果 |
| Codex 适配层 | 将实际可用的宿主事件转换为核心输入，把提醒送回 agent 上下文 |

核心不绑定 Codex 的原始事件格式。其他宿主以后根据实测能力提供适配，缺失能力应明确报告。

## 4. 编写原则：一个当前权威定义，多处引用

同一适用范围内，一个决定只有一个当前权威定义，其他位置通过身份引用它。允许为可读性作有来源的复述，但复述不能另获定义权。

需求、决策、证据、假设并非同一种事实。适用阶段、平台等范围也需要明确，避免把正常差异当成冲突。范围的具体结构化表达尚未确定。

管理单位是重要结论或条目，不强制整篇文档共用一个有效性状态。一份探路报告可以同时包含证据、候选方案和已被替代的假设。

采用渐进纳管：普通 Markdown 可自由存在；跨文档复用、影响实现或需要追溯的重要结论才要求稳定身份及关系。未纳管内容必须与已检查内容区分，不能将其计入一致性保证。

已确认的模块接口等工程契约可以在自身范围内具有约束权，不将所有设计一概视为无规范权的说明。

## 5. 候选 Markdown 协议

这是语法讨论示例，不是已经冻结的 parser 规范。倾向采用 Markdown 正文加薄层结构化声明，不发明自然语言关键词识别规则。

```markdown
## 存储方案

<!-- spec
id: DEC-STORAGE
status: active
depends_on:
  - REQ-OFFLINE
-->

采用 SQLite 保存本地数据。

<!-- /spec -->
```

- `id`：稳定条目身份。
- `status`：候选、当前有效、退役等生命周期；最终枚举尚未冻结。
- `depends_on`：本条目成立或执行所依赖的条目。
- 显式起止标记：确定条目内容范围。

runtime 计算正文及相关结构字段的指纹。标记未变但正文变化，同样属于条目变更。不从正文中的“建议”“已审查”等词推断状态。

可以有限规范化排版来降低误报，但纯代码不承诺判断语义等价。

## 6. 依赖声明与关系语义

依赖由 agent 作为作者显式声明。skill 的判断标准：如果 A 的内容改变，B 可能需要重新检查，那么 B 应声明依赖 A。

| 关系 | 语义 | 变化处理 |
| --- | --- | --- |
| `references` | 背景、出处、进一步阅读 | 检查引用有效性，不默认触发复核 |
| `depends_on` | 成立条件或执行依据 | 依据版本变化后要求复核 |

新建、实质性修改和归并探路结果时，都应重新审视依赖，既可以添加也可以删除失效依赖。

替代、挑战和证据关系具有价值，但其精确字段及生效规则仍待设计。提案声明拟替代某个决定，不能立即使旧决定失效；应在采纳后才发生替代。不能仅凭新文档更晚就覆盖旧决定。

普通引用允许互相指向。循环检查必须按关系语义定义，不能禁止整张图中的所有环。

## 7. 生命周期与版本复核分离

生命周期与复核状态是两个维度：已生效的设计仍可能因依赖变化而待复核。不能用永久的 `reviewed: true` 表示有效审查。

复核记录需要表达：B 的某个版本已经针对 A 的某个版本完成检查。倾向由 agent 调用工具提交结论，runtime 自动记录指纹，不要求 agent 手写 hash。

概念调用示例（不是冻结的 API）：

```text
review(
  item = DESIGN-LOCAL-STORAGE,
  result = compatible,
  reason = "上游仅修改备份位置，事务设计不受影响",
  snapshot = 本次检查返回的快照编号
)
```

runtime 必须验证复核期间相关版本是否再次变化，拒绝用旧快照解除新问题。并行 agent 工作时尤其需要避免覆盖新变化。

新声明依赖不等于已完成复核。可以将创建条目与显式复核提交合并为一次操作，但不能自动把当前 hash 当成已审查证据。

倾向将复核记录保存在仓库内独立的结构化文件中，具体存储、并发与事务方式尚未确定。自动同步索引不能顺手更新复核基线。

runtime 能校验版本与记录完整性，不能证明 agent 的语义判断正确或复核充分。

## 8. 变化传播

对于 C 依赖 B、B 依赖 A：

1. A 变化，B 直接待复核。
2. C 可以显示“上游链存在未解决复核”，不直接宣称 C 内容错误。
3. B 复核确认无需改变，C 不一定需要重审。
4. B 的相关内容或契约也变化后，再要求 C 复核。

传播应区分直接基线失效与间接不确定性，避免一次变化导致整个项目无差别重审。

## 9. 提醒机制：文件状态驱动

不以 SessionStart、resume、compact 作为核心提醒条件。会话事件与文档是否需要对齐没有稳定关系，runtime 不猜测 agent 正准备实现哪个模块。

hook 提供检查机会，受管文档状态决定是否提醒：

1. 工具调用结束后，由宿主 hook 触发轻量扫描。
2. 检测受管 Markdown 的新增、删除和内容变化，更新图及问题。
3. 有新增、升级或到期需再次提醒的事项时，通过支持上下文反馈的 hook 返回短报告。
4. agent 根据 skill 继续修改或显式复核。
5. 后续检查自动确认问题是否已解决；回合结束边界汇总剩余问题。

不只关注名称为“编辑”的工具：shell、脚本和 subagent 同样可能修改文件。以扫描实际文件状态为依据，不依赖解析命令来猜测副作用。具体工具覆盖需实测。

编辑中允许暂时不一致，普通待复核事项以提醒为主。回合结束不等于项目完成；明确结构错误才考虑阻塞，具体策略待定，不让普通历史 warning 无限阻止回复。

问题处理状态与提醒送达状态分开持久化。已提醒不等于已解决。同一问题避免每次调用都刷屏，可根据状态变化、检查次数和结束边界再次提醒；具体阈值未定。

MVP 优先考虑 hook 驱动的增量扫描，不先引入常驻文件监听。监听器只能发现变化，不能单独把消息送入模型上下文。没有可用 hook 时，仅靠 skill 与 MCP 工具无法保证 agent 主动调用或收到提醒，需明确能力降级。

## 10. runtime 的处理流程

```text
宿主事件 / 显式工具调用
    -> 扫描受管文件变化
    -> 解析条目、范围和显式关系
    -> 校验结构与引用
    -> 比较版本并更新图
    -> 对照复核记录生成或解除问题
    -> 持久化问题及送达状态
    -> 适配层返回精简提醒或检查结果
```

确定性检查首先关注身份唯一、字段合法、引用存在、生命周期有效、依赖版本及复核快照。元数据与缓存自身的写入应避免造成提醒循环；具体实现待设计。

## 11. 配套 skill 的工作闭环

- 澄清需求时区分已确认意图与待验证技术假设。
- 探路前读取当前有效的相关约束与既有契约。
- 探路结果交代确认了什么、推翻了什么、建议改变什么、影响哪些现有条目，并写入显式关系。
- 主 agent 归并结论，区分补充细节、工程方案调整和产品约束变化。
- 在已有授权范围内自主采纳工程决定；需要用户取舍的事项集中提出。
- 更新当前条目与任务，保留历史材料及替代来源。
- 收到 runtime 提醒后依据具体版本复核，提交处理结论，不以批量刷新 hash 代替审查。

简单项目不强制生产提案、决策、规范三套文件。最少可以在一份 Markdown 中管理多个条目。

## 12. 保证与非保证

runtime 能检查已声明关系中的断链、重复身份、失效目标、版本变化及缺失复核记录。

runtime 不能确定：

- agent 是否漏掉真实依赖。
- 两段没有显式关联的正文是否矛盾。
- 复核结论是否正确。
- agent 的当前真实工作范围是什么。

检查通过只能表示“已声明的结构和关系通过相应规则”，不能表示全部文档语义一致。未来可增加显式任务范围输入以过滤提醒，但不能把它当作真实工作范围的证明。

## 13. 实现前需要验证的事项

优先验证 Codex 接入，而不是先承诺 hook 效果：

1. 当前目标 Codex 版本、桌面端和运行环境是否支持所需事件。
2. 工具调用后的 hook 是否覆盖实际使用的工具及 subagent 场景。
3. hook 返回内容是否进入后续模型请求，而不只是显示为日志。
4. 回合结束检查是否能要求继续处理，如何避免无限循环。

此前查到官方文档搜索索引列出相关 hook，但正文访问返回 403；本次未完成宿主行为实测。参考入口：https://developers.openai.com/codex/hooks 。这些能力目前属于待验证条件，不是本项目已经确认的实现事实。

其他待定项：最终 Markdown 语法、状态枚举、替代/挑战关系、条目版本规范化、复核 API、并发存储、提醒阈值、阻塞规则和具体技术栈。

## 14. 第一版验证场景

使用一个真实闭环检验价值：

1. 当前设计 B 显式依赖决定 A，并有版本复核记录。
2. 探路结果促使 agent 修改 A。
3. agent 没有主动调用检查，宿主 hook 仍触发扫描并送达 B 待复核的提醒。
4. agent 复核 B，提交绑定快照的处理结论；若期间 A 再变更，旧复核不能解除新事项。
5. 下一次读取当前上下文时得到现行结论，同时能追溯旧决定。

并验证无变化时静默、同一问题不刷屏、普通引用不触发无差别重审，以及不把未纳管文档宣称为检查通过。

## 15. 与旧 MVP 的关系

旧 MVP 的稳定 ID、依赖图、版本基线和 agent 检查结果等思路仍可参考，但其文档分类、命令集合、技术栈、跨生态适配及阶段清单没有整体继承为当前要求。

本轮将个人宽松工作流、显式依赖、配套 skill 和文件状态驱动的提醒闭环放在优先位置。后续应依据本文继续细化，再编写新的实现规格，不直接执行归档文件的任务清单。

