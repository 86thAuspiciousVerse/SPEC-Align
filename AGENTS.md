# Spec Align 工作区指引

## 当前依据与目录

- 首先阅读根目录 `SPEC-ALIGN-CURRENT.md`。它是当前设计讨论基线，不是冻结的实现规格；保留其中的待定项，不将示例语法当作最终接口。
- 当前实施顺序与验收依据见 `IMPLEMENTATION-PLAN.md`，最终结果见生成后的 `ACCEPTANCE.md`；每阶段通过验证后再更新完成状态。
- `archive/` 是历史资料，不是当前实施依据。引用旧结论时必须核对当前基线，不直接执行旧 MVP 的任务清单。
- `codegraph/` 是借鉴对象。允许只读研究；未经用户要求，不修改其源码、不将其自动视为 Spec Align 的实现目录。进入该目录前阅读其 `AGENTS.md`，进入子目录时遵守更具体的指引。
- 新设计讨论不得静默覆盖已确认约束；明确区分证据、建议、已采纳决定和待验证假设。

## 产品与工程边界

- 个人使用优先，首先适配 Codex；核心与宿主适配层分离，不预设其他 harness 具备相同能力。
- runtime 为纯代码，不集成 LLM，不分析 agent 对话来推测意图，不从自然语言自动推断依赖或冲突。
- 依赖由作者显式声明，配套 skill 指导 agent 编写和复核；runtime 负责解析、结构检查、版本比较、影响传播和持久化。
- 文档以重要条目为管理单位，渐进纳管。检查通过不代表未声明依赖完整或全部自然语言语义一致。
- 生命周期与版本复核分离；复核绑定条目和依赖快照，不通过自动刷新 hash 假装完成审查。
- 以受管文档状态驱动提醒。hook 只是触发和送达通道，不以会话生命周期猜测业务工作状态。
- 对 Codex hook 的事件覆盖、上下文送达和阻塞能力，必须先验证目标环境，不能将文档描述直接当作本机实测结果。
- 先验证最小闭环，避免提前引入完整生态适配、复杂工作流或常驻服务。

## 工作方式

- 用户要求讨论或研究时，保持只读研究；获得实施任务后再写产品代码。用户明确要求的文档记录可直接完成。
- 复杂任务开始前识别可独立探索或审查的部分，向用户简短说明。
- subagent 用于只读探索和独立审查，不用于执行、编辑或构建。涉及三个及以上独立文件或目录的探索优先交给只读探索 agent。
- 主 agent 完成执行；完成代码功能后必须由新上下文的 subagent 独立审查。不要用同一上下文自审替代独立审查。
- 排查从最近接口开始，先确认输入输出，再深入内部。文档中的负面结论也是待验证假设；证据不足时写“当前未解决”。
- GitHub 操作优先使用 GitHub MCP；只有不可用或失败时才回退到 HTTP/CLI。读取本地 codegraph 源码直接使用本地工具。
- 研究结论应提供具体文件位置，区分可借鉴机制、不可直接迁移的假设和待实验问题。
- 不为纯文档更新运行无关构建或测试；实现变更执行与风险匹配的验证。

## 文档维护

- 当前结论尽量维护在明确的当前入口，新增专题文档时链接回该入口，避免产生多个未标识的权威版本。
- 归档文件须标注历史身份并指向当前入口，保留原始内容以便追溯。
- 协议和工具未实现前，不伪造 runtime 已验证、已审查或 hook 已送达的状态。
- 用户在当前对话中的明确指示优先于本文件的工作约定。

## 仓库管理

- 本仓库使用本地 Git，主分支 `main`。开始任务先检查状态，保留用户未提交改动。
- 按用途提交明确的文件集合；提交前先运行相关验证并检查结果，再单独执行提交，不把失败测试与提交串成一组无条件执行的命令。
- `codegraph/`、运行日志、生成产物与 `.specalign/` 不纳入提交。复核账本不是可随意丢弃的缓存，需单独备份。
- 远程仓库、推送及发布须由用户明确要求，不从本地仓库管理推断外部发布授权。

